// simple formula to get random number between min and max values

const min = 10;
const max = 20;
//Math.floor((Math.random() * (max - min + 1)) + min) // => formula
for(let i = 0; i < 20; i++) {
    
    console.log(Math.floor((Math.random() * (max - min + 1)) + min));
    
}


// string methods

